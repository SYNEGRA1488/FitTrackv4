# 🔐 БЕЗОПАСНОСТЬ: Как защитить API ключи и данные

**Проблема:** Если выложить API ключи на GitHub, они могут быть украдены и использованы против вас.

**Решение:** Никогда не коммитить `.env` файлы в Git. Использовать Vercel для хранения секретов.

---

## ⚠️ ЧТО МОЖНО УКРАСТЬ

| Данные | Риск | Последствие |
|--------|------|------------|
| **OPENAI_API_KEY** | 🔴 КРИТИЧНЫЙ | Кто-то будет использовать ваш API, счет пойдет вам |
| **DATABASE_URL** | 🔴 КРИТИЧНЫЙ | Доступ ко всем данным пользователей (пароли, emails) |
| **JWT_SECRET** | 🔴 КРИТИЧНЫЙ | Создание поддельных токенов, вход без пароля |
| **ADMIN_EMAIL** | 🟡 ВАЖНО | Целевые атаки на администратора |

---

## ✅ ПРАВИЛЬНО (КАК НУЖНО)

### 1. `.gitignore` защищает локальные файлы

**Файл: `.gitignore`**
```
# ✅ ЗАЩИЩАЕМ:
.env                      # Локальный dev файл
.env.local               # Локальный файл
.env.production          # НИКОГДА не коммитим
.env.production.local    # НИКОГДА не коммитим
.env.*.local            # Любые локальные env файлы
```

**Статус:** ✅ Уже добавлено

### 2. Секреты хранятся в Vercel

**Где хранить:**
- OPENAI_API_KEY → Vercel Environment Variables (защищены)
- DATABASE_URL → Vercel Environment Variables (защищены)
- JWT_SECRET → Vercel Environment Variables (защищены)

**Как добавить:**

```bash
# Способ 1: Через Vercel Dashboard
# https://vercel.com/dashboard → Settings → Environment Variables
# Добавить переменные вручную

# Способ 2: Через CLI
vercel env push .env.production
```

### 3. GitHub содержит только публичный код

```
GitHub содержит:
✓ HTML, CSS, JavaScript код
✓ Конфигурация (next.config.js)
✓ Документацию
✓ package.json

GitHub НЕ содержит:
✗ .env файлы (защищены .gitignore)
✗ API ключи
✗ Database URL
✗ Пароли
✗ Приватную информацию
```

---

## ❌ НЕПРАВИЛЬНО (ЧЕГО ИЗБЕГАТЬ)

### ❌ Ошибка 1: Коммитить .env файл

```bash
# НЕПРАВИЛЬНО:
git add .env.production
git commit -m "Add production env"
git push
# ❌ ТЕПЕРЬ ВСЕ ВИДЯТ ВАШИ КЛЮЧИ!
```

**Последствия:**
- GitHub сканит и находит ключи
- Боты ищут утекшие ключи
- Кто-то использует ваш OpenAI счет
- Кто-то получает доступ к БД

### ❌ Ошибка 2: Коммитить ключи в коде

```typescript
// ❌ НЕПРАВИЛЬНО:
const API_KEY = "sk-proj-abc123..."; // Прямо в коде!
const DB_URL = "postgresql://user:password@host"; // Видно всем!

// ✅ ПРАВИЛЬНО:
const API_KEY = process.env.OPENAI_API_KEY; // Из переменных
const DB_URL = process.env.DATABASE_URL; // Из переменных
```

### ❌ Ошибка 3: Использовать один ключ везде

```bash
# ❌ НЕПРАВИЛЬНО:
# Один OPENAI_API_KEY для dev и production
# Если украдут - теряется все

# ✅ ПРАВИЛЬНО:
# Разные ключи для dev и production
# dev.openai.com → одного ключа
# production.openai.com → другого ключа
```

---

## 🔒 ПОШАГОВАЯ ЗАЩИТА

### Шаг 1: Проверить что .env в .gitignore

```bash
# Проверим содержимое .gitignore
cat .gitignore | grep "\.env"
```

**Результат должен содержать:**
```
.env
.env.local
.env.production
.env.production.local
```

✅ Это уже сделано в вашем проекте!

### Шаг 2: Проверить что .env файлы НЕ в Git

```bash
# Проверить что не закоммичено
git status

# Если видите .env файлы - это проблема!
# Они должны быть в .gitignore
```

**Ожидаемый результат:**
```
On branch main
nothing to commit, working tree clean
# ✓ Все хорошо, .env файлы не видны
```

### Шаг 3: Если случайно закоммичено - удалить

**Если вы уже закоммичили .env файл:**

```bash
# 1. Удалить из истории Git
git rm --cached .env.production
git rm --cached .env.local

# 2. Добавить в .gitignore
# (уже сделано)

# 3. Закоммитить удаление
git add .gitignore
git commit -m "Remove .env files from git history"
git push

# ⚠️ ВАЖНО: Измените ВСЕ КЛЮЧИ в производстве!
# Так как они были видны в истории Git
```

### Шаг 4: Хранить секреты в Vercel

**На Vercel Dashboard:**

1. Перейти: https://vercel.com/dashboard
2. Выбрать проект
3. Settings → Environment Variables
4. Add New
5. Добавить:
   ```
   DATABASE_URL = postgresql://...
   JWT_SECRET = ваш-секрет
   OPENAI_API_KEY = sk-proj-...
   ```
6. Save

**Или через CLI:**
```bash
vercel env push .env.production
```

---

## 🛡️ ДОПОЛНИТЕЛЬНАЯ ЗАЩИТА

### Опция 1: GitHub Secrets (если используете CI/CD)

Если используете GitHub Actions:

```yaml
# .github/workflows/deploy.yml
env:
  OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
  DATABASE_URL: ${{ secrets.DATABASE_URL }}
```

Добавить на GitHub:
- Settings → Secrets and variables → Actions
- Add repository secret

### Опция 2: Rotate ключи регулярно

**Каждый месяц:**
1. Перейти на OpenAI → API Keys
2. Создать новый ключ
3. Обновить на Vercel
4. Удалить старый ключ

**Каждый квартал:**
1. Создать новый JWT_SECRET
2. Обновить на Vercel
3. Пересоздать deploy

### Опция 3: Ограничить доступ ключам

**OpenAI API Key:**
- Ограничить по IP (если возможно)
- Ограничить по лимиту расходов
- Мониторить использование

**Database:**
- Ограничить доступ по IP
- Использовать SSL/TLS соединение
- Регулярные бэкапы

### Опция 4: Мониторинг утечек

**GitHub может автоматически сканить:**

1. Settings → Security & analysis
2. Enable "Secret scanning"
3. GitHub будет предупреждать о утеках

**Дополнительные сервисы:**
- https://www.gitguardian.com (сканирует публичные ключи)
- https://haveibeenpwned.com (проверить пароли)

---

## 📋 ФИНАЛЬНЫЙ ЧЕКЛИСТ БЕЗОПАСНОСТИ

```
[ ] .gitignore содержит .env, .env.production
[ ] Никогда не коммитим .env файлы
[ ] Все секреты на Vercel, не в коде
[ ] OPENAI_API_KEY не в коде
[ ] DATABASE_URL не в коде
[ ] JWT_SECRET не в коде
[ ] ADMIN_EMAIL защищен
[ ] Разные ключи для dev и production
[ ] Регулярно ротирую ключи
[ ] GitHub secret scanning включен
[ ] Vercel environment variables установлены
[ ] Никому не даю доступ к .env файлам
[ ] Резервные копии ключей в безопасности
[ ] Мониторю использование API (OpenAI)
```

---

## 🎯 КРАТКАЯ ИНСТРУКЦИЯ

**Вы выкладываете на GitHub - БЕЗ .env файлов!**

```bash
# ✅ ПРАВИЛЬНО:
git add . # Добавляем все файлы
# .env файлы автоматически пропускаются (.gitignore)

git commit -m "Deploy: FiTTrack global"
git push origin main

# GitHub содержит:
# ✓ Код приложения
# ✓ Конфигурацию
# ✗ API ключи (защищены .gitignore)
# ✗ Пароли (защищены .gitignore)
```

**Вы добавляете на Vercel - С .env файлов!**

```bash
# ✅ ПРАВИЛЬНО:
vercel env push .env.production

# Vercel содержит:
# ✓ DATABASE_URL
# ✓ JWT_SECRET
# ✓ OPENAI_API_KEY
# Все защищено шифрованием и доступом
```

---

## 🚨 ЧТО ДЕЛАТЬ ЕСЛИ ПРОИЗОШЛА УТЕЧКА

**Если случайно выложили ключ:**

1. **Немедленно деактивировать ключ:**
   - OpenAI: https://platform.openai.com/api-keys → Delete
   - Database: изменить пароль
   - JWT: Обновить на Vercel

2. **Обновить везде:**
   ```bash
   # Создать новый ключ
   # Обновить на Vercel
   vercel env set OPENAI_API_KEY "новый-ключ"
   # Пересоздать deploy
   vercel redeploy
   ```

3. **Проверить логи:**
   ```bash
   vercel logs https://fit-track.eu --follow
   # Смотреть не было ли подозрительной активности
   ```

4. **Уведомить пользователей** (если украдены их данные)

---

## 📖 РЕЗЮМЕ

| Что | Где | Как | Безопасность |
|-----|-----|-----|--------------|
| **Код** | GitHub | git push | ✅ Безопасно |
| **.env файлы** | Локально + Vercel | vercel env | ✅ Защищено |
| **API ключи** | Vercel | Dashboard или CLI | ✅ Зашифровано |
| **Пароли** | Vercel | Dashboard или CLI | ✅ Зашифровано |
| **Данные БД** | PostgreSQL на Vercel | SSL/TLS | ✅ Защищено |

---

## ✅ ВЫ В БЕЗОПАСНОСТИ!

Ваш проект уже защищен:

✅ `.gitignore` содержит `.env`  
✅ `.env.production` не будет в GitHub  
✅ Secrets хранятся на Vercel  
✅ Код открытый, но ключи защищены  

**Просто не коммитьте .env файлы!** 🔒
