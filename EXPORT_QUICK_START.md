## 🚀 Экспорт БД в Excel — Быстрый Старт

### Одна команда для экспорта всех данных:
```bash
npm run export:db
```

### Результат:
📁 `Database Access/FitTrack_Database.xlsx`

**Содержит 4 листа:**
1. **Users** (3 пользователя) — профили, КБЖ, параметры
2. **Workouts** (1 запись) — тренировки
3. **AI Plans** (4 плана) — сгенерированные ИИ планы
4. **Workout Plans** (20 планов) — расписание

---

## 📋 Информация в каждом листе

### Users
```
ID | Email | Имя | Рост | Вес | Возраст | Цель | Активность | Тема | Язык | Калории | Белки | Жиры | Углеводы
```

### Workouts, AI Plans, Workout Plans
Все детали упражнений, сроки, статусы выполнения

---

## ⚙️ Автоматизация (опционально)

### Запускать каждый час в PowerShell:
```powershell
while ($true) {
    npm run export:db
    Start-Sleep -Seconds 3600
}
```

### Windows Task Scheduler (каждый день в 08:00):
1. Win + R → `taskschd.msc`
2. Create Task → Daily @ 08:00
3. Action: `C:\Program Files\nodejs\node.exe` 
4. Arguments: `scripts/export-db.js`
5. Start in: `C:\Users\klevi\Desktop\FiTTrack v2.2`

---

## 📂 Файл сохраняется в:
```
c:\Users\klevi\Desktop\FiTTrack v2.2\Database Access\FitTrack_Database.xlsx
```

**Каждый запуск перезаписывает файл** (новая версия заменяет старую).

---

## 🎯 Примеры использования

**Экспортировать сейчас:**
```bash
npm run export:db
```

**Экспортировать и открыть в Excel:**
```powershell
npm run export:db ; Start-Process "Database Access\FitTrack_Database.xlsx"
```

**Экспортировать с другой БД:**
```powershell
$env:DATABASE_URL="file:C:/other/path/db.db"
npm run export:db
```

---

📚 **Полная документация:** `EXPORT_DATABASE.md`
