# 📊 Автоматический Экспорт БД в Excel

## Обзор

Скрипт автоматически экспортирует всю БД в единый Excel файл с 4 листами:
- **Users** — профили пользователей (email, имя, вес, рост, цели, КБЖ и т.д.)
- **Workouts** — записи о тренировках
- **AI Plans** — сгенерированные ИИ планы
- **Workout Plans** — запланированные тренировки

Файл сохраняется в папку `Database Access` с красивым форматированием (синие заголовки, автомасштабирование).

## Быстрый Запуск

### Способ 1: NPM команда (рекомендуется)
```bash
npm run export:db
```

### Способ 2: Прямой запуск Node.js
```bash
node scripts/export-db.js
```

### Способ 3: С явным указанием БД
```powershell
$env:DATABASE_URL="file:C:/Users/klevi/Desktop/FiTTrack v2.2/prisma/dev.db"
node scripts/export-db.js
```

## Результат

Команда создаёт файл в папке `Database Access`:
```
Database Access/
  └── FitTrack_Database.xlsx
```

**Примечание:** Каждый запуск перезаписывает `FitTrack_Database.xlsx` (старая версия заменяется).

Если нужны разные версии, то запустите скрипт несколько раз:
```bash
npm run export:db  # FitTrack_Database.xlsx
npm run export:db  # FitTrack_Database.xlsx (перезаписано)
```

## Содержимое Листов

### Users
| Поле | Тип | Пример |
|------|-----|--------|
| ID | Текст | `cm_2r4x0z9u... ` |
| Email | Текст | `user@gmail.com` |
| Имя | Текст | `Иван` |
| Рост (см) | Число | `180` |
| Вес (кг) | Число | `75` |
| Возраст | Число | `28` |
| Цель | Текст | `lose_weight` |
| Активность | Текст | `moderate` |
| Тема | Текст | `dark-red` |
| Язык | Текст | `ru` |
| Калории | Число | `2000` |
| Белки (г) | Число | `150` |
| Жиры (г) | Число | `65` |
| Углеводы (г) | Число | `250` |
| Создано | Дата-время | `21.11.2025, 20:15:07` |
| Обновлено | Дата-время | `21.11.2025, 20:15:07` |

### Workouts
| Поле | Описание |
|------|----------|
| ID | Уникальный ID тренировки |
| Пользователь | ID пользователя |
| Название | Название тренировки |
| Дата | Дата и время |
| Длительность | В минутах |
| Упражнения | Первые 100 символов (описание) |
| Заметки | Дополнительные замечания |

### AI Plans
| Поле | Описание |
|------|----------|
| ID | Уникальный ID плана |
| Пользователь | ID пользователя |
| Цель | Текст заявленной цели |
| План | Первые 150 символов AI-плана |

### Workout Plans
| Поле | Описание |
|------|----------|
| ID | Уникальный ID |
| Пользователь | ID пользователя |
| Название | Название плана |
| Описание | Описание |
| Дата | Дата тренировки |
| Время | Время начала |
| Упражнения | Первые 150 символов |
| Длительность | В минутах |
| Выполнено | Да/Нет |
| Дата выполнения | Когда была выполнена |

## Форматирование Excel

✅ **Включено:**
- Синие заголовки с белым текстом (палитра: #1F4788)
- Автоматическое расширение столбцов
- Строки отсортированы по дате (новые сверху)
- Локализованные названия на русском
- Форматирование дат: `21.11.2025, 20:15:07`

## Как Открыть Excel

- **Windows**: Двойной клик → Microsoft Excel или Google Sheets
- **macOS**: Двойной клик → Numbers или Excel
- **Linux**: LibreOffice Calc (`libreoffice FitTrack_Database.xlsx`)
- **Online**: Google Sheets (Upload → Open)

## Автоматизация

### Запуск каждый день в 08:00 (Windows Task Scheduler)

1. Откройте **Task Scheduler** (Win + R → `taskschd.msc`)
2. "Create Task..." → назовите "FitTrack Export"
3. **Triggers** → New → Daily → 08:00
4. **Actions** → New → 
   - Program: `C:\Program Files\nodejs\node.exe`
   - Arguments: `scripts/export-db.js`
   - Start in: `C:\Users\klevi\Desktop\FiTTrack v2.2`
5. ✅ OK

Теперь файл будет обновляться каждый день в 08:00.

### Запуск каждый час (для частых обновлений)

```bash
# В PowerShell
while ($true) {
    npm run export:db
    Start-Sleep -Seconds 3600  # 1 час
}
```

## Поддержка

Если возникают ошибки:

1. **"Unable to open the database file"**
   - Убедитесь, что `prisma/dev.db` существует
   - Переопределите DATABASE_URL вручную

2. **"Column X does not exist"**
   - Запустите `npx prisma db push` для миграции схемы
   - Или используйте `npx prisma generate` для обновления типов

3. **Excel файл не создался**
   - Проверьте права доступа на `Database Access/` папку
   - Убедитесь, что `exceljs` установлен: `npm install exceljs`

## Структура Кода

```
scripts/
  └── export-db.js  ← Основной скрипт (175 строк)
      ├── 1. Подключение БД через Prisma
      ├── 2. Создание папки Database Access
      ├── 3. Экспорт 4 таблиц (Users, Workouts, AI Plans, Workout Plans)
      ├── 4. Форматирование Excel
      └── 5. Сохранение в Database Access/FitTrack_Database.xlsx
```

## Параметры

По умолчанию скрипт использует `prisma/dev.db`.

Чтобы экспортировать из другой БД:
```bash
$env:DATABASE_URL="file:C:/path/to/prod.db"
npm run export:db
```

---

**Последнее обновление:** 21 ноября 2025  
**Версия скрипта:** 2.0
